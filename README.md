# CPS Backend

## Run locally
1. Start MongoDB locally or with `docker compose up mongo -d`
2. Run `./mvnw spring-boot:run` or `mvn spring-boot:run`
3. Swagger: `http://localhost:8080/swagger-ui.html`

## Default seeded user
- username: `admin`
- password: `Admin@12345`
